{% set name = "earthkit-utils" %}
{% set version = "0.2.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name.replace('-', '_') }}-{{ version }}.tar.gz
  sha256: b939ecec9bf71ea66ee31a7d170441fdea7b99629e06529a160348f108083017


build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python {{ python_min }}
    - pip
    - setuptools >=61
    - setuptools-scm >=8
  run:
    - array-api-compat
    - python >={{ python_min }}

test:
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip
  imports:
    - earthkit.utils

about:
  home: https://github.com/ecmwf/earthkit-utils
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Utilities for the Earthkit ecosystem.
  dev_url: https://github.com/ecmwf/earthkit-utils

extra:
  recipe-maintainers:
    - sandorkertesz
    - iainrussell
